import 'package:flutter_test/flutter_test.dart'; import 'package:nomad/utils/format_detector.dart'; group('FormatDetector', () { test('detects code blocks', () { expect(FormatDetector.hasCodeBlock(''), isTrue); }); test('detects tables', () { expect(FormatDetector.hasTable('|a|b|\n|---|---|'), isTrue); }); test('detects JSON', () { expect(FormatDetector.hasJson('{k:v}'), isTrue); }); });
